name: Build Single Tag Versions
on:
  push:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Echo repository info
        run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."

      - name: Check if index.md was changed
        id: check_changes
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q "docs/resources/ethics-reading-list/index.md"; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Create topic files in repository l
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          # multi-word tags
          cd docs/resources/ethics-reading-list/
          for n in "general%20resources general-resources" "crowdsourcing%20issues crowdsourcing-issues" "dual%20use dual-use" "model%20issues model-issues" "language%20diversity language-diversity" "environmental%20impact environmental-impact"; do a=( $n ); grep t-${a[0]} index.md > x-${a[1]}.md; cat single-file-header.txt x-${a[1]}.md > t-${a[1]}.md; done
          rm -f x-*
          # single word tag
          for n in biases data evaluation uncategorized; do grep t-$n index.md > x-$n.md; cat single-file-header.txt x-$n.md > t-$n.md; done
          rm -f x-*

      - name: Create type files in repository
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          cd docs/resources/ethics-reading-list/
          for n in published preprint post report; do grep type-$n index.md > x-$n.md; cat single-file-header.txt x-$n.md > type-$n.md; done
          rm -f x-*
 
      - name: Setup git config
        run: |
          git config user.name "ACL Ethics Bibliography Actions Bot"
          git config user.email "actions@github.com"

      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git add .
          git commit -am "Auto-generate topic files for ethics reading list"
          git push origin

      - run: echo "🍏 This job's status is ${{ job.status }}."